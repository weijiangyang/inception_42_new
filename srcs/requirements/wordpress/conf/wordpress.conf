[wordpress]

user = www-data
group = www-data

listen = 9000

pm = dynamic
pm.max_children = 25
pm.start_servers = 5
pm.min_spare_servers = 1
pm.max_spare_servers = 10
pm.max_requests = 500

clear_env = yes

security.limit_extensions = .php